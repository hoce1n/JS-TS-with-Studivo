# TypeScript

What the type system adds on top of JavaScript in Studivo.

JS already answers what runs. These notes answer what the compiler knows, what it rejects, and how
Studivo names that in `.ts` / `.tsx` files.

Copy [`template.md`](template.md) for the first topic.
